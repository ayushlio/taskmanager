apiVersion: v1
kind: Service
metadata:
  name: taskmanager-service
  namespace: taskmanager
spec:
  selector:
    app: taskmanager-api
  type: NodePort
  ports:
  - port: 80
    targetPort: 8000
    nodePort: 30080
